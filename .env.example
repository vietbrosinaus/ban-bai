# Local: the PartyKit dev server from `npm run dev:party`
NEXT_PUBLIC_PARTY_HOST=localhost:1999

# Production: the host `npm run deploy:party` prints, e.g. ban-bai.<your-user>.partykit.dev
# NEXT_PUBLIC_PARTY_HOST=ban-bai.elvistranhere.partykit.dev
