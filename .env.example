# Where the table server lives.
#
# Local: the worker started by `npm run dev:party`
NEXT_PUBLIC_PARTY_HOST=localhost:1999

# Production: the host `npm run deploy:party` prints, e.g.
# NEXT_PUBLIC_PARTY_HOST=ban-bai.<your-account>.workers.dev
